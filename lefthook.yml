pre-push:
  jobs:
    - name: packages audit
      tags:
        - security
      run: npm run audit --audit-level=high

pre-commit:
  parallel: true
  commands:
    test:
      run: npm run test
      tags:
        - test
