pre-push:
  jobs:
    - name: packages audit
      tags:
        - security
      run: npm run audit

pre-commit:
  parallel: true
  commands:
    test:
      run: npm run test
      tags:
        - test
